/** Shared props for tradesman onboarding sequence (personalization + compliance links). */
export type OnboardingBaseProps = {
  firstName: string;
  tradeType: string;
  marketingPreferencesUrl: string;
  marketingUnsubscribeUrl: string;
};
