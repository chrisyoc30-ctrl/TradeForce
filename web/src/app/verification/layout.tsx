import type { ReactNode } from "react";

export const metadata = {
  title: "Verification documents",
  description:
    "Upload ID and public liability insurance for TradeScore homeowner trust badges.",
};

export default function VerificationLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
