import { LeadScoringBoard } from "@/components/leads/lead-scoring-board";

export const metadata = {
  title: "Lead scoring | TradeScore",
  description: "High-priority leads for tradespeople",
};

export default function LeadScoringPage() {
  return <LeadScoringBoard />;
}
